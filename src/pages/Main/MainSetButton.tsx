import React from 'react'
import styled from 'styled-components'

export default function MainSetButton() {
  return (
    <ButtonContainer>

    </ButtonContainer>
  )
}

const ButtonContainer = styled.span`
  display:flex;
  flex-direction:row:
  align-items:space-between;
`